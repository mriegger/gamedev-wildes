target/
node_modules/
dist/
build/
.env
.env.local
.DS_Store

# Godot 4 — editor-generated import/cache; regenerated on first open
.godot/
# export_presets.cfg is committed on purpose — build-macos-release needs the
# "macOS" preset. It uses disabled codesign, so it holds no signing secrets.
